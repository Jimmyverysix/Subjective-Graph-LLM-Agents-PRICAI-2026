本目录默认提供主实验所需的清洗后数据：

- `FINAL_full_plus_all_scores_cleaned.v2.csv`：用于构建班级社交网络与 6 次考试的动态基准真值（主入口默认读取它）。

可选（仅在你需要“从更原始表重新清洗/对齐列顺序”时）：

- `FINAL_full_plus_all_scores_cleaned.csv`：更早版本/更原始的清洗表（不随仓库强制提供；如有需要请自行放入本目录）。
